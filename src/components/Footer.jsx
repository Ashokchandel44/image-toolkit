import React from 'react';

function Footer() {
  return (
    <footer className="site-footer">
      <div className="container footer-inner">
        <p>ImageToolkit runs entirely in your browser, so uploaded images never need a server to be processed.</p>
        <p>Deploy the built files to shared hosting under `public_html/image-toolkit/`.</p>
      </div>
    </footer>
  );
}

export default Footer;
